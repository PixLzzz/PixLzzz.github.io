{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "backend",
      "runtimeExecutable": "wsl",
      "runtimeArgs": ["bash", "-c", "cd /home/pixl/Dev/AppartClaude && venv/bin/pip install httpx==0.27.0 -q && venv/bin/uvicorn main:app --host 0.0.0.0 --port 8001 --reload 2>&1"],
      "port": 8001
    },
    {
      "name": "frontend",
      "runtimeExecutable": "wsl",
      "runtimeArgs": ["bash", "-c", "cd /home/pixl/Dev/AppartClaude/frontend && npm install && npm run dev -- --host"],
      "port": 5174
    }
  ]
}
